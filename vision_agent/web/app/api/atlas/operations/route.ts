import {NextResponse} from "next/server";import {getSession} from "../../../../lib/auth";import {foodBankContext} from "../../../../lib/food-bank";import {sql,withTransaction} from "../../../../lib/db";import {fetchDisruptions,forecastDemand,haversine,severityMultiplier,type Site} from "../../../../lib/atlas/operational";
export const maxDuration=45;
export async function GET(){const session=await getSession();if(!session)return NextResponse.json({error:"Authentication required"},{status:401});try{const context=await foodBankContext(session),runs=await sql()`SELECT r.*,COALESCE(json_agg(s ORDER BY s.sequence) FILTER(WHERE s.id IS NOT NULL),'[]') steps FROM operational_runs r LEFT JOIN operational_steps s ON s.operational_run_id=r.id WHERE r.site_id=${context.siteId} GROUP BY r.id ORDER BY r.created_at DESC LIMIT 10`;return NextResponse.json({context,runs});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to load ATLAS"},{status:400});}}
export async function POST(){const session=await getSession();if(!session)return NextResponse.json({error:"Authentication required"},{status:401});try{const context=await foodBankContext(session);const siteRows=await sql()`SELECT id,name,county,state,latitude,longitude FROM sites WHERE id=${context.siteId}`,site=siteRows[0] as unknown as Site;if(!site)throw new Error("Site not found");const [disruptions,inventory,history,network]=await Promise.all([fetchDisruptions(site),sql()`SELECT category,sum(quantity)::float quantity FROM inventory_items WHERE site_id=${context.siteId} AND condition='good' GROUP BY category`,sql()`SELECT category,date_trunc('day',created_at) day,sum(quantity)::float quantity FROM inventory_transactions WHERE site_id=${context.siteId} AND direction='out' AND approval_status='approved' AND created_at>now()-interval '30 days' GROUP BY category,date_trunc('day',created_at) ORDER BY day`,sql()`SELECT s.id,s.name,s.county,s.state,s.latitude,s.longitude,i.category,sum(i.quantity)::float quantity FROM sites s JOIN inventory_items i ON i.site_id=s.id AND i.condition='good' WHERE s.id<>${context.siteId} GROUP BY s.id,i.category`]);const multiplier=severityMultiplier(disruptions.events),categories=new Set([...inventory.map(x=>String(x.category)),...history.map(x=>String(x.category))]);const forecasts=[...categories].map(category=>{const h=history.filter(x=>x.category===category).map(x=>Number(x.quantity)),model=forecastDemand(h,multiplier),onHand=Number(inventory.find(x=>x.category===category)?.quantity||0);return{category,onHand,...model,shortage:Math.max(0,model.forecast-onHand)}}).sort((a,b)=>b.shortage-a.shortage);const need=forecasts[0]||{category:"Unspecified",onHand:0,baseline:0,trend:0,forecast:0,confidence:.35,method:"no inventory history",shortage:0};const candidates=network.filter(x=>x.category===need.category).map(x=>{const source={id:String(x.id),name:String(x.name),county:String(x.county),state:String(x.state),latitude:Number(x.latitude),longitude:Number(x.longitude)};const available=Math.max(0,Math.floor(Number(x.quantity)*.75));return{...source,available,distance:haversine(source,site)}}).filter(x=>x.available>0).sort((a,b)=>a.distance-b.distance);const source=candidates[0]||null,requested=need.shortage,counter=source?Math.min(source.available,Math.ceil(requested*.75)):0,accepted=counter>=Math.ceil(requested*.6),transport=source&&counter?{fromSiteId:source.id,fromSite:source.name,toSite:site.name,category:need.category,quantity:counter,distanceMiles:Number(source.distance.toFixed(1)),estimatedMinutes:Math.max(10,Math.ceil(source.distance/35*60)),capacityStatus:"requires logistics confirmation"}:null;const output=await withTransaction(async c=>{const run=(await c.query("INSERT INTO operational_runs(site_id,organization_id,trigger_type,status,initiated_by) VALUES($1,$2,'live_network_review',$3,$4) RETURNING *",[context.siteId,context.organizationId,requested&&accepted?"awaiting_human":"completed",session.userId])).rows[0];const steps=[{agent:"Inventory Agent",input:{siteId:site.id},output:{categories:forecasts},explanation:`Audited ${inventory.length} inventory categories against approved dispatch history.`,approval:false},{agent:"Disruption + Demand Agent",input:{sources:["api.weather.gov","OpenFEMA"],events:disruptions.events.length},output:{events:disruptions.events,multiplier,forecast:need,sources:disruptions.sources},explanation:`Ran ${need.method}; live disruption multiplier is ${multiplier.toFixed(2)}×.`,approval:false},{agent:"Site Negotiation Agent",input:{requested,category:need.category},output:{source,requested,counteroffer:counter,acceptedByPolicy:accepted},explanation:source?`${site.name} requested ${requested}; ${source.name} countered with ${counter}. ${accepted?"Counter meets the 60% acceptance threshold.":"Escalated because the counter is insufficient."}`:"No eligible partner surplus was found.",approval:Boolean(counter)},{agent:"Transport Logistics Agent",input:{source:source?.id,destination:site.id},output:transport||{feasible:false,reason:"No negotiated load"},explanation:transport?`Estimated ${transport.distanceMiles} miles and ${transport.estimatedMinutes} minutes; a logistics human must confirm vehicle capacity.`:"No route was proposed.",approval:Boolean(transport)},{agent:"ATLAS Orchestrator",input:{precedingSteps:4},output:{recommendation:transport?"Review coordinated transfer":"No transfer recommended",requested,counteroffer:counter,humanDecisionRequired:Boolean(transport)},explanation:"Combined inventory, disruption, demand, negotiation, and logistics evidence without executing a commitment.",approval:Boolean(transport)}];for(let i=0;i<steps.length;i++){const s=steps[i];await c.query("INSERT INTO operational_steps(operational_run_id,agent_name,sequence,status,input,output,explanation,requires_human_approval) VALUES($1,$2,$3,$4,$5,$6,$7,$8)",[run.id,s.agent,i+1,s.approval?"awaiting_human":"completed",s.input,s.output,s.explanation,s.approval]);}for(const e of disruptions.events)await c.query("INSERT INTO disruption_events(site_id,source,external_id,event_type,severity,headline,starts_at,ends_at,payload) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9) ON CONFLICT(site_id,source,external_id) DO UPDATE SET severity=EXCLUDED.severity,headline=EXCLUDED.headline,payload=EXCLUDED.payload,fetched_at=now()",[site.id,e.source,e.id,e.type,e.severity,e.headline,e.startsAt||null,e.endsAt||null,e.payload]);if(transport)await c.query("INSERT INTO transport_plans(operational_run_id,from_site_id,to_site_id,category,quantity,distance_miles,estimated_minutes) VALUES($1,$2,$3,$4,$5,$6,$7)",[run.id,transport.fromSiteId,site.id,transport.category,transport.quantity,transport.distanceMiles,transport.estimatedMinutes]);await c.query("UPDATE operational_runs SET summary=$2,completed_at=now() WHERE id=$1",[run.id,{forecast:need,events:disruptions.events.length,requested,counteroffer:counter,transport}]);return{...run,steps,summary:{forecast:need,events:disruptions.events.length,requested,counteroffer:counter,transport}}});return NextResponse.json({run:output},{status:201});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"ATLAS run failed"},{status:500});}}
