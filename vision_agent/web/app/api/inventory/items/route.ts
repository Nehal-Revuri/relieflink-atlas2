import {NextResponse} from "next/server";import {z} from "zod";import {getSession} from "../../../../lib/auth";import {foodBankContext,canEditInventory} from "../../../../lib/food-bank";import {sql} from "../../../../lib/db";
const Item=z.object({productName:z.string().min(1).max(200),brand:z.string().max(120).nullable().optional(),category:z.string().min(1).max(80),subcategory:z.string().max(100).nullable().optional(),quantity:z.number().nonnegative(),unit:z.string().min(1).max(40),lotNumber:z.string().max(100).nullable().optional(),expirationDate:z.string().nullable().optional(),warehouseZone:z.string().max(100).nullable().optional(),binLocation:z.string().max(100).nullable().optional(),condition:z.enum(["good","damaged","quarantined","expired"]).default("good"),sourceName:z.string().max(160).nullable().optional(),barcode:z.string().max(100).nullable().optional(),notes:z.string().max(1000).nullable().optional(),intakeMethod:z.enum(["manual","csv","vision"]).default("manual"),visionConfidence:z.number().min(0).max(1).nullable().optional()});
export async function GET(){const session=await getSession();if(!session)return NextResponse.json({error:"Authentication required"},{status:401});try{const context=await foodBankContext(session),items=await sql()`SELECT * FROM inventory_items WHERE site_id=${context.siteId} ORDER BY expiration_date NULLS LAST,product_name`;return NextResponse.json({context,items});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to load inventory"},{status:400});}}
export async function POST(request:Request){const session=await getSession();if(!session)return NextResponse.json({error:"Authentication required"},{status:401});try{const context=await foodBankContext(session);if(!canEditInventory(context.role))throw new Error("Inventory edit permission required");const x=Item.parse(await request.json());const rows=await sql()`INSERT INTO inventory_items(organization_id,site_id,product_name,brand,category,subcategory,quantity,unit,lot_number,expiration_date,warehouse_zone,bin_location,condition,source_name,barcode,notes,intake_method,vision_confidence,created_by,updated_by) VALUES(${context.organizationId},${context.siteId},${x.productName},${x.brand??null},${x.category},${x.subcategory??null},${x.quantity},${x.unit},${x.lotNumber??null},${x.expirationDate||null},${x.warehouseZone??null},${x.binLocation??null},${x.condition},${x.sourceName??null},${x.barcode??null},${x.notes??null},${x.intakeMethod},${x.visionConfidence??null},${session.userId},${session.userId}) RETURNING *`;return NextResponse.json({item:rows[0]},{status:201});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Invalid inventory item"},{status:400});}}
