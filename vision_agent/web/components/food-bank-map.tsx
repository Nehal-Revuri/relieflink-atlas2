"use client";import {MapContainer,Marker,Popup,TileLayer,useMap} from "react-leaflet";import L from "leaflet";import {useEffect} from "react";type Bank={id:string;name:string;address:string;county:string;state:string;latitude:string;longitude:string;agent_name:string|null;inventory_units:string};const icon=L.divIcon({className:"bank-marker",html:"<span>R</span>",iconSize:[36,36],iconAnchor:[18,18]});function Fit({banks}:{banks:Bank[]}){const map=useMap();useEffect(()=>{if(banks.length)map.fitBounds(banks.map(b=>[Number(b.latitude),Number(b.longitude)] as [number,number]),{padding:[45,45],maxZoom:11});},[banks,map]);return null;}export default function FoodBankMap({foodBanks}:{foodBanks:Bank[]}){return <MapContainer center={[37.77,-122.25]} zoom={9} className="network-map"><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><Fit banks={foodBanks}/>{foodBanks.map(b=><Marker key={b.id} position={[Number(b.latitude),Number(b.longitude)]} icon={icon}><Popup><strong>{b.name}</strong><br/>{b.address}<br/>{b.county} County, {b.state}<hr/>{Number(b.inventory_units).toLocaleString()} units<br/>{b.agent_name||"Agent not configured"}</Popup></Marker>)}</MapContainer>}
